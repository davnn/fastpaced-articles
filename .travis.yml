language: generic
sudo: false
script:
- "TRAVIS_TOKEN=$TRAVIS_TOKEN && curl -s -X POST -H 'Content-Type: application/json' -H 'Accept: application/json' -H 'Travis-API-Version: 3' -H 'Authorization: token $TRAVIS_TOKEN' -d '{\"request\": {\"branch\":\"master\"}}' https://api.travis-ci.org/repo/davnn%2Fcuriousobserver/requests"
